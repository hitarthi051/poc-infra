version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
  build:
    commands:
      - sudo apt update && sudo apt install -y jq
      - chmod +x buildspec/dynamic-pipeline.sh
      - bash buildspec/dynamic-pipeline.sh